---
name: preview
description: Start a local HTTP dev server at port 8000 for live site preview
---

Run `python3 -m http.server 8000` from the repo root to start a local HTTP server. Open http://localhost:8000 in a browser to preview the site. The server serves static files from the repo root directly. Kill with Ctrl+C when done.
