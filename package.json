{
  "name": "usfellows",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "validate": "npm run build && node scripts/validate-build.mjs",
    "smoke": "node scripts/smoke-test.mjs",
    "visual:sitemap": "node scripts/generate_visual_sitemap.js http://127.0.0.1:4321 --root=dist --out=public/visual-sitemap"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource/cormorant-garamond": "^5.2.11",
    "@fontsource/source-sans-3": "^5.2.9",
    "@lucide/astro": "^1.23.0",
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "playwright": "^1.61.1",
    "typescript": "^6.0.3"
  }
}
