Full-resolution visual-sitemap captures are local QA artifacts and must not be
committed. `npm run visual:sitemap` captures pages in memory and writes only
800-pixel-wide AVIF thumbnails to this directory.
